import tensorflow as tf

# Here we will call the train_step function
# For every pair image text we will call to get text embedding and then call train_step
def train():
    return None


# Every step we give it a images and a batch of text embeddings
# First we generate image from text embeddings after applying noise on it
# Then we give the generated image and text embeddings to the discriminator
# Discriminator gives us a decision
# We calculate the loss and backpropagate it
def train_step():
    return None
